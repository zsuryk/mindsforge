import { PlaceholderPage } from "@/components/placeholder-page";

export default function JobsPage() {
  return (
    <PlaceholderPage
      title="Jobs"
      note="Job submission and pipeline status will land with the ingestion ticket."
    />
  );
}