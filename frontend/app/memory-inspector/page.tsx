import { PlaceholderPage } from "@/components/placeholder-page";

export default function MemoryInspectorPage() {
  return (
    <PlaceholderPage
      title="Memory Inspector"
      note="Persistent creator memory will land with the memory inspector ticket."
    />
  );
}